- Inspect the `.docx` file as a valid DOCX package. Never read the entire binary file directly with `read_file`. Use document-aware methods to manage content, ensuring that the structure is not compromised. Also, use libraries like `python-docx`, `zipfile`, or `unzip` to read and modify individual XML parts instead of handling raw binary data directly.
- After modifying the `.docx` file, verify that the saved output can be opened and is a valid ZIP container. Ensure that the essential content (located in `word/document.xml`) is present and correctly formatted, free from errors or missing placeholders.
- Ensure the document’s XML structure is valid: confirm that all placeholders and control markers are correctly filled or removed according to specifications. Only then proceed to generate `fill_report.json`.